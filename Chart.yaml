apiVersion: v2
name: templates
description: Opinionated application chart that wraps helm-core. Provides Deployment, Service, ConfigMap, CronJob, and HPA resources — configured entirely through values, no template authoring required.
type: application
version: 0.2.0
dependencies:
  - name: core
    version: "0.2.0"
    repository: "oci://ghcr.io/yagelhayun/helm-charts"
