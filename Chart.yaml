apiVersion: v2
name: templates
description: Opinionated application chart that wraps helm-core. Configured entirely through values, no template authoring required.
type: application
version: 0.4.0
dependencies:
  - name: core
    version: 0.3.3
    repository: oci://ghcr.io/yagelhayun/helm-charts
