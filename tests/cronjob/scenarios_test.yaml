suite: cronJob — opt-in, configuration, and schema
templates:
  - templates/cronjob.yaml
tests:

  # ── Opt-in ───────────────────────────────────────────────────────────────────

  - it: does not render when cronJob.enabled is false
    values:
      - ../_shared/common.yaml
    set:
      cronJob.enabled: false
    asserts:
      - hasDocuments:
          count: 0

  - it: renders a CronJob when cronJob.enabled is true
    values:
      - ../_shared/common.yaml
    set:
      cronJob.enabled: true
    asserts:
      - hasDocuments:
          count: 1
      - isKind:
          of: CronJob

  # ── Behavior ─────────────────────────────────────────────────────────────────

  - it: uses the configured schedule
    values:
      - ../_shared/common.yaml
    set:
      cronJob.enabled: true
      # cronJob.schedule=@hourly from common.yaml
    asserts:
      - equal:
          path: spec.schedule
          value: "@hourly"

  - it: defaults concurrencyPolicy to Forbid when not set
    values:
      - ../_shared/minimal.yaml
    set:
      cronJob.enabled: true
      cronJob.schedule: "0 * * * *"
    asserts:
      - equal:
          path: spec.concurrencyPolicy
          value: Forbid

  - it: defaults suspend to false when not set
    values:
      - ../_shared/common.yaml
    set:
      cronJob.enabled: true
    asserts:
      - equal:
          path: spec.suspend
          value: false

  - it: sets suspend to true when configured
    values:
      - ../_shared/common.yaml
    set:
      cronJob.enabled: true
      cronJob.suspend: true
    asserts:
      - equal:
          path: spec.suspend
          value: true

  # ── Schema validation ────────────────────────────────────────────────────────

  - it: schema rejects non-boolean suspend
    values:
      - ../_shared/minimal.yaml
    set:
      cronJob.enabled: true
      cronJob.schedule: "0 * * * *"
      cronJob.suspend: "yes"
    asserts:
      - failedTemplate: {}

  - it: accepts a standard 5-field schedule
    values:
      - ../_shared/minimal.yaml
    set:
      cronJob.enabled: true
      cronJob.schedule: "0 2 * * 1-5"
    asserts:
      - hasDocuments:
          count: 1

  - it: accepts @hourly macro
    values:
      - ../_shared/minimal.yaml
    set:
      cronJob.enabled: true
      cronJob.schedule: "@hourly"
    asserts:
      - hasDocuments:
          count: 1

  - it: schema rejects a 6-field schedule (year not supported)
    values:
      - ../_shared/minimal.yaml
    set:
      cronJob.enabled: true
      cronJob.schedule: "0 0 * * * 2025"
    asserts:
      - failedTemplate: {}

  - it: schema rejects an invalid schedule string
    values:
      - ../_shared/minimal.yaml
    set:
      cronJob.enabled: true
      cronJob.schedule: "not-a-cron"
    asserts:
      - failedTemplate: {}

  - it: schema rejects unknown @ macro
    values:
      - ../_shared/minimal.yaml
    set:
      cronJob.enabled: true
      cronJob.schedule: "@reboot"
    asserts:
      - failedTemplate: {}

  - it: schema rejects cronJob.enabled true without schedule
    values:
      - ../_shared/minimal.yaml
    set:
      cronJob.enabled: true
    asserts:
      - failedTemplate: {}

  - it: schema rejects invalid concurrencyPolicy
    values:
      - ../_shared/minimal.yaml
    set:
      cronJob.enabled: true
      cronJob.schedule: "0 * * * *"
      cronJob.concurrencyPolicy: Invalid
    asserts:
      - failedTemplate: {}

  - it: schema rejects negative successfulJobsHistory
    values:
      - ../_shared/minimal.yaml
    set:
      cronJob.enabled: true
      cronJob.schedule: "0 * * * *"
      cronJob.successfulJobsHistory: -1
    asserts:
      - failedTemplate: {}

  - it: schema rejects negative failedJobsHistory
    values:
      - ../_shared/minimal.yaml
    set:
      cronJob.enabled: true
      cronJob.schedule: "0 * * * *"
      cronJob.failedJobsHistory: -1
    asserts:
      - failedTemplate: {}
