# Workload explicitly disabled — used to verify workload templates do not render
# and that resources/replicas are not required when workload.enabled: false.
global:
  region: us-east-1
  ignoreLookup: "true"

workload:
  enabled: false

service:
  enabled: false
configMap:
  enabled: false
hpa:
  enabled: false
